{
  "name": "play-node-native-binding",
  "version": "1.0.0",
  "main": "entry.js",
  "scripts": {
    "dev": "yarn build && yarn test",
    "build": "node-gyp rebuild",
    "build-electron": "node-gyp rebuild --target=v8.3.4 --dist-url=https://atom.io/download/electron",
    "test": "node ./test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^3.0.0",
    "node-gyp": "^7.0.0"
  }
}
